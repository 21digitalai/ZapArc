// Floating menu reserved for future wallet shortcuts.
// The legacy page prompt and blacklist flows were removed.

export default class FloatingMenu {
  constructor() {
    console.log('ZapArc floating menu is disabled');
  }
}
